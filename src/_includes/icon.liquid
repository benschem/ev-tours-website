{% comment %}
  Usage: {% include "icon.liquid", name: "sun", class: "icon sun-icon icon-sm", id: "sun-icon" title: "Activate light mode." %}
  For a decorative icon (hidden from screen readers):
  {% include "icon.liquid", name: "sun", class: "icon icon-sm", aria_hidden: true %}
{% endcomment %}

<span
  {% if id %} id="{{ id }}" {% endif %}
  class="{{ class }}"
  {% if tooltip %} title="{{ tooltip }}" {% endif %}
  {% if aria_hidden %} aria-hidden="true" {% endif %}
>
  {% include "icons/{{ name | escape }}.svg" %}
</span>

{% comment %} TODO - stop this breaking if svg file does not exist {% endcomment %}
